policies:
  - name: stop-instances-with-custodian-tag
    resource: gcp.instance
    filters:
      - type: value
        key: name
        op: regex
        value: custodian.*
    actions:
      - stop
