policies:
  - name: my-first-policy
    resource: gcp.sql-instance-database
    query:
      - instance: custodian-sql
    filters:
      - type: value
        key: name
        op: regex
        value: ^custodian$
    actions:
      - type: delete
